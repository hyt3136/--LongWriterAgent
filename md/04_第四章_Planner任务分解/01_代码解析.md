# 第四章代码解析：Planner节点与任务分解

本章交付：
- 输入规范化
- 可执行计划生成
- 结构化任务依赖

代码文件：
- code/src/wenben_engine/planner/service.py

关键说明：
1. normalize_request 把脏输入规范化成稳定字段。
2. plan_project 输出结构化 tasks，而不是自然语言段落。
3. 每个任务都包含 depends_on，为后续路由和校验准备基础数据。
